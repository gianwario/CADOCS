from utils import Tools

class ToolSelector:

    def execute_tool(self, tool, entities):
        if(tool == Tools.CsDetector):
            #TODO: execute csDetector
            return ["CS1", "CS2", "CS3"]